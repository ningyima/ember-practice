<article class="listing">
  <div class="details">
    <div>
      <span>{{this.fund.title}}</span>
      {{#if (not this.fund.selected)}}
        <a onclick={{action "add" this.fund}} role="button">Add to Cart</a>        
      {{/if}}
    </div>
  </div>
</article>
